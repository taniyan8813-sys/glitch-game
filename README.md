# GLITCH SURVIVE

GitHub Pages ready build of the browser game.

## Run locally

Open `index.html` in a browser. No external server or build step is required.

## GitHub Pages

Use the repository root as the Pages source. The top page is `index.html`.

Required runtime files:

- `index.html`
- `style.css`
- `script.js`
- `assets/`

